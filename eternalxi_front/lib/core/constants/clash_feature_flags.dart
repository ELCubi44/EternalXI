/// Flags de acceso al modo Clash desde la app.
abstract final class ClashFeatureFlags {
  /// `false` oculta el acceso en selección de modo hasta que Clash esté listo.
  static const modeSelectionEnabled = true;
}
